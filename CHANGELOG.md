# 更新日志

## 3.1.0（未发布）

- 重新启动应用后首次进入回到「总览」，不再从持久存储恢复上次页签和滚动位置；当前页面存活期间仍保留切页、返回与刷新的浏览位置。
- 云存档仅手动同步：取消进入前台和每小时自动触发；打开页面只读取本地记录，不自动应用后台下载结果。手动同步时只处理当前账号的待同步结果。

- Dataset 与 Blob 已知大小的读取最多并行两块，按偏移拼装；继续验证长度、EOF 和大小限制，失败时等待在途请求结束后关闭句柄。

- 同步时在后台 Worker 计算推分结果并随存档保存；旧数据首次异步补算，按账号、成绩指纹和算法版本复用持久缓存，损坏或过期缓存自动重建，成绩渲染与图片生成不再现场重算。
- 点击跨页标签只准备起止页面，保留横向过渡，避免进入定数表时额外构建成绩页；Base64 解码优先使用原生接口并保留旧 WebView 兼容路径。

- 首次进入从总览读取本地摘要，成绩与曲库按页加载；本地浏览不读取登录凭据，凭据迁移延后至账号写入或同步。
- 避免初始化时重复构建默认页及隐藏相邻页，保留页签与滚动位置；失败可重试，刷新仍读取最新 Dataset。

## 3.0.0（2026-09-18）

- Debug 改为手动推送 `debug-v<版本号>` 标签发布；日常 CI 只验证并上传构建产物，不再创建滚动发布。

- 保存页签与滚动位置，被宿主释放或应用重启后可恢复浏览上下文。

- 打开页面时优先展示本地账号与成绩摘要，再读取曲库和完整分析数据，减少首屏等待。

### 界面与交互

- 页面整理为总览、成绩、时间线、定数表四个入口；成绩内切换 RKS 列表与 B30，账号管理独立打开，保留 TapTap 登录、手动令牌、备注和删除范围提示。
- 四个业务页签均分整行，保持指示器、点击与横向翻页同步。
- 总览增加当前账号本地 RKS 历史变化图，支持 7 天、30 天、90 天和全部范围及节点查看；处理空历史、单点、无效记录和下降曲线，不生成虚构历史。
- 难度统计分别展示 EZ/HD/IN/AT 的 Clear、FC、AP；总览不增加 B30 或推分目标快捷卡。
- 定数表恢复整数定数分组、定数/曲名与作曲者或 ID/难度三列布局；补充曲库作曲者字段解析，保留分页。
- RKS 与定数表直接多选 EZ/HD/IN/AT，默认 IN、AT，不增加“全部”或单独筛选面板；搜索后仍保留全局 RKS 排名，B30 不重复标注分组序号。
- 成绩与定数表各自保留搜索和难度选择，避免预加载页面的选中态与实际筛选条件不一致。
- 同步期间允许浏览本地页面与历史，写入类操作仍防止重复提交；沿用原 Dataset、凭据存储和 RKS 算法。

- 业务标签页支持跟手左右翻页、短拖回弹与点按横向过渡，选中态和指示器同步；保留各页滚动位置、成绩／定数分页状态，避免页面控件相互绑定。
- 提示框改为统一主题反色表面，无左侧色条及绿色底块。
- 时间线默认折叠，只显示日期、RKS 和变化数量；点击后按需加载全部变化详情。
- 修复部分 EZ 成绩页被长曲名撑宽的问题，约束卡片网格最小宽度，而非裁切页面。
- 恢复原生版难度／评级／RKS 的深浅领域配色，保留个人简介换行，窄屏分行展示资料，并用两列与分隔线区分推分 ACC 和单曲 RKS。
- 相同成绩的重复同步不再新增时间线；仍记录首次同步、成绩或 FC 改善、RKS／课题变化。旧的无变化节点仅从展示中过滤，保留原始历史数据。
- 操作反馈固定在当前视口底部，可关闭并自动消失。
- 补齐 Web 页面中的存档概况、难度统计、成绩评级与推分 ACC、P3/B27 分区、时间线变化明细与范围选择、定数分组与曲库更新、账号备注编辑。
- B30 改为双列图，个人信息图与 B30 均先预览再通过系统文件保存器导出；取消令牌对话框不再受必填校验阻挡。
- 成绩／定数列表按页渲染，推分目标按需计算并缓存；统一宿主主题 token、错误色与加载按钮状态，移除重复应用标题。
- 增加曲库解析、筛选、历史标签与时间范围的 Node 契约测试。
- 对齐迁移前原生版与 UI 规范：下划线页签、合并玩家／RKS 摘要、四列难度统计、带排名的紧凑成绩卡；修复 WebView 预览弹窗高度，曲绘不可用时明确提示文字回退。
- 修复前台和 Worker 对宿主大网络响应 `bodyBlob.id` 的读取与清理；曲绘下载限制并发并合并重复请求，避免临时响应耗尽存储配额。增加分块响应及失败清理测试。
- 将交付格式从同进程 API1 `plugin.apk` 迁移为 format v3 Web/Worker 插件，不再以宿主身份运行普通业务代码。
- 复用 `profiles`、`analysis-data`、`session-tokens` 与 `song-catalog` Dataset ID 和格式，安装时由宿主按 Copy -> Validate -> Switch -> Retain 原子导入 2.x 数据。
- SessionToken 写入宿主加密 SecretStore；网络、文件保存和后台任务均通过可撤销 Capability。
- 迁移总览、RKS、B30、时间线、定数表、TapTap 登录、云存档解析和 PNG 导出，并增加 Web RKS、ZIP、存档解析与登录加密 fixture。
- 修正云存档列表 URL，并把 TapTap 文件 CDN 纳入最小网络 scope，避免规范化重定向或下载域名被宿主安全策略拒绝。
- 最低宿主 versionCode 提升到 24，需要 `file.export` 与 Runtime Contract 2.1。

## 2.3.2 - 2026-08-28

### 兼容性

- 基于插件 SDK `1.4.0` 重新构建并将最低宿主版本提升到 23；本版本在完成 V3 迁移并通过迁移、恢复、业务与降级测试前继续作为稳定 API1 实现，不新增旧 API 能力。
- 保持 profiles、analysis-data、session-tokens 与 song-catalog 的 Dataset v1 导出／恢复契约，供 Runtime v2 单向迁移使用。

## 2.3.1（2026-08-21）

### 新增特性

- 正式提供账号档案、成绩与分析历史、SessionToken 和曲库缓存的独立导入、导出和删除。
- 导入前报告各 Dataset 是否已有数据；四类 Dataset 均采用显式替换，不提供可能破坏关联关系的合并。

### 安全与可靠性

- SessionToken 在导出时从旧 Keystore 解密并按用户选择进入密码区或明文区，导入后使用目标安装的 Android Keystore 重新加密。
- 文件数据先在私有目录暂存并验证路径、大小和摘要，再通过目录切换替换旧数据。

### 兼容性

- 基于正式插件 SDK `1.3.1` 构建，最低宿主版本提升到 22；本版本是 Bridge 预发布线验证后的正式版本。

## 2.3.0-bridge.5（2026-08-21）

### 优化

- 重新基于插件 SDK `1.3.0-bridge.5` 构建，跟随宿主完整的浅色／深色容器色阶，避免共享界面回退到默认蓝紫色。

### 兼容性

- 最低宿主版本提升到 21。

## 2.3.0-bridge.4（2026-08-21）

### 新增特性

- 导入前分别探测账号档案、成绩与分析历史、SessionToken 和曲库缓存是否已有数据，供宿主显示对应处理选项。
- 四类 Dataset 明确声明为仅支持替换；导入界面会禁用合并并提示只能跳过或替换，避免把替换实现误当作合并。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.4`，最低宿主版本提升到 20。

## 2.3.0-bridge.3（2026-08-20）

### 新增特性

- 数据管理支持分别删除账号档案、成绩与分析历史、SessionToken 和曲库缓存，并在删除后执行状态校验。

### 安全与可靠性

- 删除 SessionToken 时保留非敏感账号档案；无凭据档案不再被误报为可导出的凭据 Dataset，后续重新登录可只补回对应账号的令牌。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.3`，最低宿主版本提升到 19；该版本仅投放 Debug 渠道。

## 2.3.0-bridge.2（2026-08-14）

### 新增特性

- Migration Bridge 支持恢复账号档案、分析文件、曲库缓存与 SessionToken；凭据使用当前安装的 Android Keystore 重新加密。

### 安全与可靠性

- 分析文件先解压到私有暂存目录并检查路径和体积，再以目录切换替换旧数据；账号、凭据和小部件状态写入后均执行一致性检查。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.2`，最低宿主版本提升到 18；该版本仅投放 Debug 渠道。

## 2.3.0-bridge.1（2026-08-14）

### 新增特性

- Debug 预发布接入只读 Migration Bridge，分 Dataset 导出账号档案、分析数据、SessionToken 与曲库缓存。

### 兼容性

- 编译基线更新为临时插件 SDK `1.3.0-bridge.1`，最低宿主版本提升到 17；该版本仅投放 Debug 渠道。

## 2.2.1（2026-08-11）

### 新增特性

- 六个标签页支持左右滑动切换，与点击标签等价。页内横向滚动的筛选条仍然优先响应，滑到尽头才会翻页。

### 修复

- 难度与评级配色补齐深色档位，EZ／HD／IN／AT 与 PHI 金在深色主题下保持可读；RKS 涨跌改用应用语义色。

### 优化

- 空态、间距与圆角改用共享设计系统，B30 计算口径与历史记录说明默认收起。

### 兼容性

- 编译与测试基线同步到 Android Tool Suite 插件 SDK 1.2.0，继承应用选择的主题和默认／动态配色。

## 2.2.0（2026-08-07）

### 新增特性

- `main` 分支测试和构建成功后自动更新公开的 `debug` 预发布，可直接进入宿主调试仓库。

### 优化

- 正式 Release 与调试预发布统一生成带通道和提交号的发布元数据，并在完成后事件驱动更新插件索引。

## 2.1.0（2026-07-24）

### 新增特性

- 接入官方插件仓库与 GitHub Release 自动发布流程。

### 兼容性

- 插件包升级为 format v2，声明整数版本号、最低宿主版本 11 和插件 SDK 1.1.0。

## 2.0.6

### 说明

- 本文件从自动发布版本开始维护；更早版本历史仍可通过 Git 提交记录查询。
